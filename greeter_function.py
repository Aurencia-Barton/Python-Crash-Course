def greet_user(username): # defines a function definition named greet_user().
    
    """Display a simple greeting message.""" # begins the body of the function with a docstring that describes what the function does.
    print(f"\nHello, {username.title()}\n") # Line of actual code that executes the function's task.
    
greet_user('jessie') #it finally, calls the function greet_user() to execute the code within it.